apiVersion: v1
kind: Secret
metadata:
  name: "{{ .Release.Name }}-appsettings-dev"
  namespace: {{ .Release.Namespace }}
type: Opaque
data:
  appsettings.json: {{ .Files.Get "configs/prd/appsettings.json" | b64enc | quote }}
